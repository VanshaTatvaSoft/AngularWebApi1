import { FormatCurrencyCompactPipePipe } from './format-currency-compact-pipe-pipe';

describe('FormatCurrencyCompactPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatCurrencyCompactPipePipe();
    expect(pipe).toBeTruthy();
  });
});
